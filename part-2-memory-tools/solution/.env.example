# AWS credentials are managed via Teleport
# Before running the agent, export the required environment variables:
#
# export AWS_ACCESS_KEY_ID="your-access-key"
# export AWS_SECRET_ACCESS_KEY="your-secret-key"
# export AWS_CA_BUNDLE="/path/to/ca-bundle.pem"
# export HTTPS_PROXY="http://127.0.0.1:port"
#
# The agent uses eu.amazon.nova-lite-v1:0 model in eu-central-1 region
